<h1>All Users</h1>
<div class="library">
<% @users.each do |user|  %>
	<%= link_to(user, class: "book-item ") do%>
		<h2><%= user.name %> </h2>
		<p><b>Admin:</b>	<%= user.admin %></p>
		<p><b>Created-at:</b> <%= user.created_at %></p>
		
	<% end %>
<% end %>
</div>
